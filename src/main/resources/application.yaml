spring:
  application:
    name: crud
  datasource:
    url: jdbc:mysql://localhost:3306/java-training
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
